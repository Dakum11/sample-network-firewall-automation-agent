[project]
name = "aws-network-firewall-automation-app"
version = "1.0.0"
description = "Web interface for the AWS Network Firewall Automation Agent."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.40.53",
    "flask>=3.0.0",
    "gunicorn>=22.0.0",
]
